---
문서: 통합분석 리포트 구성 기준
적용범위: index.html #cmbResult (통합분석 탭 결과 리포트)
최종수정: 2026-08-20
---

## 0. 핵심 원칙

(현재 없음 — 통합분석은 애초에 1인 정보만 입력받아 분석을 돌리는 구조라 궁합보기와 겹칠 여지가 구조적으로 없다. 궁합 리포트 구성 문서 같은 "경계선" 원칙은 불필요. 대화하다가 정말 필요한 원칙이 생기면 그때 추가한다.)

## 1. 리포트 구성 순서 (신규 개편)

**Zone1 · 관상x사주 캐릭터 유형 — 기존 유지**
`classifyAndBuildCharacter` ([js/ai-analysis.js:2394](js/ai-analysis.js:2394)) — 관상 70 + 사주 30 융합 룰베이스 판정(`computeCharacterResult`). 사주 원국(`pillars`)이 있으면 자동으로 융합되는 기존 로직 그대로 둔다.

**Zone2 · 관상x사주 총평 — 히어로 영역 (신규)**
- 헤드 메시지 + 총점 노출
- **총점 = 관상x사주 케미 점수(시너지 지수)** [확정] — `computeCharacterResult`가 이미 관상(`faceResult.raw`)과 사주(`sajuResult.raw`)를 같은 6기질 벡터 공간에 산출해두므로([js/character/character-engine.js:217-221](js/character/character-engine.js:217)), 두 벡터의 축별 편차를 근거로 "관상과 사주가 얼마나 같은 방향을 가리키는지"를 점수화한다. 사진만 있고 사주가 없는 등 한쪽 데이터가 없으면 궁합보기 `heroScores.gwansang: null` 패턴과 동일하게 케미 점수는 `null` 처리.
- **헤드 메시지 = 룰베이스** — 궁합보기 `ggHeadline`(`buildCoupleHeadline`)과 같은 패턴. 케미 점수 구간(예: 상/중/하 3~4단계)별로 고정 문구 템플릿을 미리 써두고 점수에 따라 골라 쓴다. (아래 §2 판단 기준 참고)
- **총평 = AI** — 그 점수가 왜 그렇게 나왔는지 설명하는 서술만 AI가 생성. 궁합보기 `ggHeroReason`과 동일한 역할.

**Zone3 · 관상x사주 데이터 풀이** (2026-08-21 2차 개편 — 실제 데이터로 목업하며 확정)
- **페어1 · 사주 원국** — `renderPillarsTable`이 이미 그리는 화면 그대로(12운성·신살·귀인 기둥별 표시, 커스텀 UI 없음). 관상 6기질은 넣지 않음 — Zone1 캐릭터 판정 근거에 이미 있어서 중복이라 뺐다. → 하단에 융합 풀이 1개(`zone3_manseryeok_reading`, 3~4문장, 관상 6기질 점수는 화면에 없어도 AI 근거로는 계속 씀).
- **페어2 · 오행능력치 비교** — 관상%(좌)·관상바·오행 라벨(중앙)·사주바·사주%(우)로 마주보게 배치, 오행별 색 통일(목=초록·화=빨강·토=노랑·금=회색·수=파랑). 한줄평(사주 한 줄 + 관상 한 줄)은 그래프 위가 아니라 아래, 각자 영역에 가운데 정렬로 노출 — 사주 쪽은 기존 `OHAENG_TITLE`(20자+, 다른 탭과 공유) 대신 `FACE_OHAENG_TITLE`와 길이를 맞춘 전용 `OHAENG_TITLE_SHORT`를 써서 두 박스 높이가 어긋나지 않게 함. → 하단에 융합 풀이 1개(`zone3_ohaeng_reading`, 3~4문장).
- **페어3 · 대운x삼정 인생 타임라인** — 갑자 한자 나열 대신, 대운 각 구간의 12운성(일간 대비)을 이름+짧은 설명으로 세로 목록에 표시. 삼정(상정/중정/하정 %)은 별도 그래프가 아니라 나이 구간(초년~29세·중년30~59세·말년60세~ — Zone4 초년/중년/말년운과 경계 통일) 제목으로 목록 안에 녹임. 현재 나이가 속한 구간에 "지금" 태그, 12운성이 장생·관대·건록·제왕(기운 상승기)일 때 "⭐ 좋은 시기" 태그. → 하단에 융합 풀이 1개(`zone3_daeun_reading`, 3~4문장).
- 세 풀이 모두 **AI 생성**(문장 수는 스키마에 3~4문장으로 지시하지만, LLM 특성상 매번 정확히 지켜지진 않음 — 코드로 강제하는 하드 제약은 아니라 어느 정도 변동은 있을 수 있음).

**Zone4 · 관상x사주 스토리 풀이 (신규)**
- 카드 1(고정): "OO의 인생의 흐름을 살펴본다면" — OO은 이름(성 제외). 초년운(~29세)/중년운(30~59세)/말년운(60세~) 3단 구성, 각 단마다 짧은 헤드 카피(`early_life_headline` 등, **반드시 한 줄 — "주변의 기대를 품고 스스로의 기준을 단단하게 다져가던 시기" 정도를 넘지 않게**) + 나이대 표기 + 본문. 관상+사주 데이터를 반드시 함께 근거로 쓰도록 필드 설명에 명시(관상만/사주만으로 쓰지 말 것).
- 카드 2~N(가변): 후보 주제 6개 — 가족(부모/형제/자식운), 일(어울리는 직장·업무스타일·사업가 기질), 돈(저축/소비 스타일, 돈에 대한 마음가짐), 사랑(추구하는 연애·결혼운), 대인관계, 쉼/힐링(어울리는 여행지·스타일). **전체 카드 수는 최소 7개** — 6개 주제 전부 최소 1개씩 나오되, "주제당 정확히 1개씩(총 6개)"로 끝나면 안 되고 최소 1개 주제는 2~3개로 나눠야 한다. 예) 가족1 + 일1 + 돈3(할 말 많음) + 사랑1 + 대인관계1 + 쉼힐링2 = 총 9개. (2026-08-21: 처음엔 "최소 6개"였는데 실제로 6개(주제당 1개)만 나오는 경우가 흔해서 최소 7개로 올림 — 그래도 이 이상의 확장 여부는 AI 판단이라 정확한 개수를 코드로 강제하진 못함.)
- **카드 제목 = AI 자유 생성(톤 참고 예시 있음)** — ⚠️ 2026-08-21 정정: 원래 "후보 문구 뱅크에서 고르거나 살짝 변형"하는 하이브리드로 설계했는데, AI가 참고 예시 문장을 그대로 베껴 쓰는 문제가 실제로 발생했다(§3에 적어둔 말투 예시를 그대로 코드에 후보뱅크로 넣은 게 원인). 이제는 그 예시들을 "톤 참고용"으로만 프롬프트에 주고, 그대로/살짝만 바꿔 쓰는 걸 명시적으로 금지 — 카드의 실제 풀이 내용에서 나온 표현으로 매번 새로 짓게 한다. (§2 판단 기준 3번 갱신)
- 각 카드는 **풀이(reading, 3~4문장) + "왜 이렇게 풀이했나요?"(basis)** 2단 구성 — 이 본문은 AI 자유 생성. 궁합보기 Zone2 아이템의 `reading`/`basis` 패턴을 그대로 재사용 ([js/ai-analysis.js:1826-1827](js/ai-analysis.js:1826)).

## 2. AI vs 룰베이스 판단 기준

궁합보기 화면을 실측해보니 실제로 AI가 쓰이는 곳은 3곳뿐이었고(`ggHeroReason`, `ggZone1AiShape`, `ggZone2AiItems`의 reading/basis), 제목·숫자·헤드라인처럼 화면 구조를 이루는 요소는 전부 룰베이스(조건분기 + 미리 쓴 템플릿, 또는 lookup table)였다. 궁합보기 코드 주석([js/ai-analysis.js:160-162](js/ai-analysis.js:160))의 원칙을 그대로 가져온다:

> AI에게 숫자를 맡기면 화면에 이미 떠 있는 값과 어긋날 수 있다 — 그래서 AI는 "왜 그런지 설명하는 글"만 쓰고, 숫자·제목 등 구조를 이루는 값은 만들지 않는다.

**적용 규칙**
1. **숫자·판정·제목처럼 다른 로직이 참조하거나 화면 구조를 이루는 값 → 룰베이스.** 같은 입력이면 항상 같은 값이 나와야 한다(Zone1 캐릭터 판정이 대표 사례).
2. **그 값을 풀어서 설명하는 서술(본문) → AI.** 조합의 경우의 수가 너무 많아 사람이 다 써둘 수 없는 경우.
3. **"제목"인데 그 자체가 개인화된 재미 포인트인 경우(Zone4 카드 제목처럼) → AI 자유 생성 + 톤 참고 예시.** 후보 문구를 "고를 수 있는 선택지"로 주면 AI가 그대로 베껴 쓴다(실제 발생한 문제) — 예시는 톤/형식 참고용으로만 주고, 그대로/살짝 변형 사용은 명시적으로 금지해야 진짜 새 문장이 나온다.
4. 구현하면서 위 기준으로 봤을 때 AI보다 룰베이스가 맞다고 판단되는 지점이 나오면, 이 문서에 있는 "AI 생성" 표시와 다르더라도 룰베이스로 바꾼다. (구현 중 발견되는 예외는 이 문서를 그때그때 갱신)

## 3. AI 생성 지침

**Zone4 카드 제목 (AI 자유 생성 — 아래 예시는 "톤 참고용"일 뿐, 후보 목록이 아니다)**
- 아래 말투 예시는 절대 그대로 쓰거나 단어만 바꿔 쓰지 말 것 — 반드시 그 카드의 실제 풀이 내용에서 나온 표현으로 매번 새로 지을 것(2026-08-21: 후보뱅크로 줬다가 AI가 그대로 베낀 문제 발견 후 수정)
- 2030 타깃, MBTI 풀이처럼 이해가 쉽게
- **전문용어 사용 금지** — 현침살, 자충수, 천정이 밝다, 재백궁 등 사주/관상 전문 용어를 그대로 노출하지 않고 풀어서 표현
- **자음 표현(ㄴㄴ, ㄱㄱ 등) 사용 자체를 금지**
- 글자 수 기준은 아직 정하지 않음 — 실제로 생성해보고 너무 길면 추후 기준을 추가
- 단어 선택/뉘앙스/말투 예시 (아래는 참고용, 위 규칙이 항상 우선):
  - 누가 뭐래도 당신은 모두가 기댈 수 있는 든든한 산
  - 재물 바다를 품은 거대한 태산, 스케일이 다른 K-여사장님 재질
  - 알잘딱깔센 재테크의 신, 근데 통제는 살짝 힘들어함
  - 사장님 할 거 아니면 답답해서 어찌 사시나? 팩폭 들어갑니다
  - 돈 복은 타고났네, 머니 파이프라인 심는 재능 만렙
  - 연애는 프리스타일, 결혼은 비혼주의? No, 숨은 인연 찾기
  - 아버지는 태평양, 어머니는 등대... 나는 그 사이의 섬
  - 서울보다는 강릉, 해외는 캐나다? 따스한 햇살과 숲이 필요해
  - 애증의 K-가족, 그래도 내 편인 거 알지?
  - 인싸인 듯 아싸, 아싸인 듯 인싸
  - 역마살? ㄴㄴ 글로벌 인재임. 해외로 튀어! (※ 이 예시의 "ㄴㄴ" 자체는 위 자음 표현 금지 규칙에 걸리므로 실제 생성 시 제외)

**Zone4 카드 분리 규칙**
- 후보 주제 6개(가족/일/돈/사랑/대인관계/쉼힐링)는 전부 최소 1개 카드로 나온다 — "이 사람에게 안 맞는 주제라 아예 뺀다"는 없음.
- 한 주제 안에 내용이 많으면 그 주제만 최대 3개까지 세부 카드로 분리한다. 예: 돈 풀이에서 사업가 기질이 강하게 나오면 "돈"과 "사업"을 별도 카드로, 사랑 풀이에서 연애운과 결혼운이 각각 분량이 많으면 따로 분리.
- 전체 가변 카드 수는 **최소 7개** — 주제당 정확히 1개씩(총 6개)으로 끝내면 안 되고, 최소 1개 주제는 2~3개로 나눠야 한다.
- 최종 카드 개수 = 고정 항목(인생의 흐름) 1개 + 가변 항목(주제 6개, 총 7~18개).

**Zone4 이유(basis) 영역**
- 각 카드 풀이(reading) 아래에 "왜 이렇게 풀이했나요?" 영역을 두고, 어떤 사주x관상 데이터 근거로 이렇게 풀이했는지 서술한다 (궁합보기 화면의 `basis` 필드와 동일 패턴).
