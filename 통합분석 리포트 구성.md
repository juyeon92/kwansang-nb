---
문서: 통합분석 리포트 구성 기준
적용범위: index.html #cmbResult (통합분석 탭 결과 리포트)
최종수정: 2026-08-21
---

## 0. 핵심 원칙

(현재 없음 — 통합분석은 애초에 1인 정보만 입력받아 분석을 돌리는 구조라 궁합보기와 겹칠 여지가 구조적으로 없다. 궁합 리포트 구성 문서 같은 "경계선" 원칙은 불필요. 대화하다가 정말 필요한 원칙이 생기면 그때 추가한다.)

## 1. 리포트 구성 순서 (신규 개편)

**Zone1 · 관상x사주 캐릭터 유형 — 기존 유지, 원칙상 캐릭터 판정만**
`classifyAndBuildCharacter` ([js/ai-analysis.js:2594](js/ai-analysis.js:2594)) — 관상 70 + 사주 30 융합 룰베이스 판정(`computeCharacterResult`). 사주 원국(`pillars`)이 있으면 자동으로 융합되는 기존 로직 그대로 둔다. ⚠️ 2026-08-21 6차 개편: 부위별 관상 풀이(`part_deep_dive`)를 한때 여기에 뒀다가, "Zone1은 원칙상 무조건 관상 캐릭터만 다룬다"는 사용자 확인으로 Zone3로 옮겼다(아래 페어1.5 참고) — Zone1엔 다른 콘텐츠를 섞지 않는다.

**Zone2 · 관상x사주 총평 — 히어로 영역** (2026-08-22 8~9차 개편 — 같은 점/다른 점 + 오행 에너지 비교 추가·이동은 §8, 최종 총평 위치 원복·해시태그 제목은 §9 참고)
- 헤드 메시지 + 총점 노출
- **총점 = 관상x사주 케미 점수(시너지 지수)** [확정] — `computeCharacterResult`가 이미 관상(`faceResult.raw`)과 사주(`sajuResult.raw`)를 같은 6기질 벡터 공간에 산출해두므로([js/character/character-engine.js:217-221](js/character/character-engine.js:217)), 두 벡터의 축별 편차를 근거로 "관상과 사주가 얼마나 같은 방향을 가리키는지"를 점수화한다. 사진만 있고 사주가 없는 등 한쪽 데이터가 없으면 궁합보기 `heroScores.gwansang: null` 패턴과 동일하게 케미 점수는 `null` 처리. 화면 표기는 "점"이 아니라 "%"(관상·사주 싱크) — 8차 개편에서 의미가 더 직관적으로 읽히도록 단위만 바꿨다(계산 로직은 그대로).
- **헤드 메시지 = 룰베이스** — 궁합보기 `ggHeadline`(`buildCoupleHeadline`)과 같은 패턴. 케미 점수 구간(예: 상/중/하 3~4단계)별로 고정 문구 템플릿을 미리 써두고 점수에 따라 골라 쓴다. (아래 §2 판단 기준 참고)
- **최종 통합 총평 = AI** — 그 점수가 왜 그렇게 나왔는지 설명하는 서술(`zone2_review`)만 AI가 생성. 궁합보기 `ggHeroReason`과 동일한 역할. 케미 점수 카드 안(기운 줄다리기 바 바로 아래)에 붙어 있다 — 8차 개편에서 잠깐 Zone2 맨 아래로 옮겼다가, 9차 개편에서 "점수 밑에 바로 연결해서 보고 싶다"는 요청으로 원위치했다(§9 참고). 내용·필드는 그대로.
- **둘이 같은 점 / 다른 점(8차 개편, 신규)** — `zone2_common_points`(같은 점 2~3개)·`zone2_different_points`(다른 점 1~2개, face/saju 대비 쌍)를 케미 점수 카드 바로 다음에 불릿으로 노출. §8 참고.
- **오행 에너지 비교(8차 개편, Zone3에서 이동)** — 원래 Zone3 페어2였던 그래프+융합풀이를 통째로 여기로 옮겼다. 같은 점/다른 점 다음, Zone2 맨 마지막 섹션. §8 참고.

**Zone3 · 관상x사주 데이터 풀이** (2026-08-21 2차 개편 — 실제 데이터로 목업하며 확정. 3차 개편에서 "개인 기질" 서사는 Zone4 고정카드2로 옮겨서, 아래 두 풀이는 순수 비교로 되돌림 — §4 참고. 8차 개편으로 페어2 "오행능력치 비교"는 Zone2로 이동했다 — §8 참고)
- **페어1 · 사주 원국** — `renderPillarsTable`이 이미 그리는 화면 그대로(12운성·신살·귀인 기둥별 표시, 커스텀 UI 없음). 관상 6기질은 넣지 않음 — Zone1 캐릭터 판정 근거에 이미 있어서 중복이라 뺐다. → **바로 아래에 `sinsal_reading`(2026-08-21 5차 개편, 신규) 배치** — 원국표에 뱃지로만 나열되던 신살·귀인이 몇 개나 있는지, 그 개수·조합 자체가 어떤 의미인지(예: 신살이 많으면 "인생에 전환점·굴곡이 잦은 편") 풀어주는 종합 해석. 사주탭에서도 쓰던 필드인데 통합분석엔 담을 곳이 없어 버려지고 있었다(사용자 피드백: "살이 이렇게 많은데 이게 무슨 의미인지 풀이가 없다"). → 그 아래에 융합 풀이 1개(`zone3_manseryeok_reading`, 3~4문장) — [사주 원국]↔[관상 6기질 점수]만 비교, 신강/신약 판단은 Zone4 고정카드2("나의 기질")로 옮김.
- **페어1.5 · 관상 정보 (2026-08-21 6차 개편, 신규)** — `part_deep_dive`(관상탭에서 쓰던, 눈·코·미간 등 실측 데이터 기반 부위별 4~6개 상세해설: 쉬운 풀이→관상 분석→전통 원리→현실 조언)를 사주 원국 바로 다음, 대운x삼정 바로 위에 배치(오행능력치 비교가 Zone2로 이동하면서 8차 개편 이후로는 사주 원국 바로 다음이 됐다). Zone1은 캐릭터 판정만 다룬다는 원칙 때문에 Zone1이 아니라 여기로 옮겼다. `renderPartDeepDive`가 관상탭과 완전히 같은 카드 HTML(`partDeepDiveCardHtml`)을 재사용해 톤을 통일한다. 이 페어는 다른 페어들과 달리 AI 융합풀이(zone3_*_reading)가 따로 없다 — part_deep_dive 카드 자체가 이미 풀이+근거 구조를 갖고 있어서다.
- **페어2 · 대운x삼정 인생 타임라인** — 갑자 한자 나열 대신, 대운 각 구간의 12운성(일간 대비)을 이름+짧은 설명으로 세로 목록에 표시. 삼정(상정/중정/하정 %)은 별도 그래프가 아니라 나이 구간(초년~29세·중년30~59세·말년60세~ — Zone4 초년/중년/말년운과 경계 통일) 제목으로 목록 안에 녹임. 현재 나이가 속한 구간에 "지금" 태그, 12운성이 장생·관대·건록·제왕(기운 상승기)일 때 "⭐ 좋은 시기" 태그. → 하단에 융합 풀이 1개(`zone3_daeun_reading`, 3~4문장).
- 풀이는 **AI 생성**(문장 수는 스키마에 3~4문장으로 지시하지만, LLM 특성상 매번 정확히 지켜지진 않음 — 코드로 강제하는 하드 제약은 아니라 어느 정도 변동은 있을 수 있음).

**Zone4 · 관상x사주 스토리 풀이** (2026-08-21 5차 개편 — "조언" 카드를 Zone4 맨 마지막으로 이동)

Zone4는 세 블록으로 나뉜다: **오프닝 고정 카드(3개, "나는 누구인가")** → **가변 카드(6주제 총 8개, "생활 속의 나")** → **클로징 고정 카드(1개, "조언")**. 리포트를 다 읽은 다음 마지막에 조언으로 마무리하는 구조(2026-08-21 사용자 요청 — 원래는 오프닝 고정카드 묶음 안에 있었는데, "이제는 이렇게 해보세요"는 전체를 다 본 뒤에 나와야 자연스럽다는 피드백으로 위치를 바꿈).

### 오프닝 고정 카드 (3개, 전부 등장 — AI가 뺄 수 없음)

| 순서 | 카드 | 내용 | 제목 | 근거 |
|---|---|---|---|---|
| 1 | 인생의 흐름을 살펴본다면 | 초년(~29세)/중년(30~59세)/말년(60세~) 3단, 각 단 헤드카피+본문 (기존 유지) | 고정("📖 인생의 흐름을 살펴본다면") | 관상+사주 함께, 대운×삼정 |
| 2 | 나의 기질 | 오행 구조(zero/과다)를 비유로 풀어 이 사람의 에너지 밸런스를 설명 (예: "토 제로라 뿌리내릴 곳 없는 나무") | 고정("⚖️ 나의 기질과 에너지 밸런스") | 사주: 오행분포+`[신강/신약]`+`[용신]` / 관상: 관상 오행분포·6기질 |
| 3 | 남이 모르는 내 모습 | 관상 겉인상과 사주(오행·십성)에서 드러나는 실제 성향이 다를 때의 반전 포인트 | 고정("🎭 남이 모르는 내 모습") | 사주: 오행·십성(편인/정인 등 섬세함) / 관상: 전체 인상(강해보임 등) vs 눈매·부위 |

- 오프닝 고정 카드 2~3의 **제목은 룰베이스(고정 문구)**다 — §2 판단 기준 1번(구조를 이루는 값은 룰베이스) 적용. 카드1의 제목이 이미 `renderZone4Card1`에 고정 코딩돼 있는 것과 같은 이유.
- 카드2·3은 각각 **`reading`(3~5문장) + `basis`("왜 이렇게 풀이했나요?")** 2단 구성 — 가변 카드와 같은 패턴.
- 카드1만 예외적으로 헤드카피+본문 3단 구성을 유지(기존 구조 그대로, basis 없음).

### 가변 카드 (6주제, family·love는 각 2장, 총 8개)

| 순서 | 주제 | 카드 | 처리 | 근거 |
|---|---|---|---|---|
| 5 | family(가족) | 자라온 환경 | 기존 유지 | 오행/십성 전반 + 관상 6기질 |
| 6 | family(가족) | 부모님과의 관계 | 신규 | `[십성 목록]` 재성(아버지)·인성(어머니) 유무 |
| 7 | relationships(대인관계) | 대인관계 | 보강 | 화개살·귀문관살·겁살/재살 + 눈썹·미간 |
| 8 | love(사랑) | 매력·인기 | 신규 — love의 첫 카드로 배치 | 홍염살·년살 + 도화안·난안(관상 눈 유형) |
| 9 | love(사랑) | 연애·결혼 스타일 | 기존 유지 — love의 두 번째 카드로 배치 | 일간·오행 + 관상 6기질 |
| 10 | work(일) | 일 | 보강 | `[십성 목록]` 조합(상관/편관/편인 등) |
| 11 | money(돈) | 돈 | 보강 | 재성(편재/정재) + 코끝·입 |
| 12 | rest(쉼/힐링) | 쉼/힐링 | 신규 근거 | `[용신]` 부족 오행 채우기 |

- family·love는 반드시 2개 이상 — 나머지 4주제는 최소 1개, 할 말이 많으면 최대 3개까지 세부 분리 가능.
- **love의 카드 순서는 "매력·인기" → "연애·결혼 스타일"** 순으로 고정(2026-08-21 확정 — 가볍게 시작해서 구체적인 스타일로 들어가는 흐름).
- 카드 제목은 (고정 카드와 달리) **AI 자유 생성** — §2 판단 기준 3번, §3 참고.

### 클로징 고정 카드 (1개, 맨 마지막)

| 순서 | 카드 | 내용 | 제목 | 근거 |
|---|---|---|---|---|
| 13 | 조언 | 강점은 살리고 과한 부분은 줄이는 현실적 행동 조언 | 고정("🧭 이제는 이렇게 해보세요") | 기존 `growth_guidance` 필드 재활용(관상탭·사주탭에도 이미 있던 필드) + 통합분석에서는 Zone3·Zone4 데이터를 근거로 보강 |

- Zone4 전체(오프닝 3 + 가변 8)를 다 지나간 뒤 맨 마지막에 온다 — 리포트를 다 읽고 나서 받는 마무리 조언이라는 의도.
- 오프닝 고정 카드와 같은 `reading`(3~5문장) + `basis` 2단 구성, 제목도 동일하게 룰베이스.

**총 카드 수 = 오프닝 고정 3개 + 가변 8개 + 클로징 고정 1개 = 최소 12개.**

## 2. AI vs 룰베이스 판단 기준

궁합보기 화면을 실측해보니 실제로 AI가 쓰이는 곳은 3곳뿐이었고(`ggHeroReason`, `ggZone1AiShape`, `ggZone2AiItems`의 reading/basis), 제목·숫자·헤드라인처럼 화면 구조를 이루는 요소는 전부 룰베이스(조건분기 + 미리 쓴 템플릿, 또는 lookup table)였다. 궁합보기 코드 주석([js/ai-analysis.js:159-162](js/ai-analysis.js:159))의 원칙을 그대로 가져온다:

> AI에게 숫자를 맡기면 화면에 이미 떠 있는 값과 어긋날 수 있다 — 그래서 AI는 "왜 그런지 설명하는 글"만 쓰고, 숫자·제목 등 구조를 이루는 값은 만들지 않는다.

**적용 규칙**
1. **숫자·판정·제목처럼 다른 로직이 참조하거나 화면 구조를 이루는 값 → 룰베이스.** 같은 입력이면 항상 같은 값이 나와야 한다(Zone1 캐릭터 판정, Zone4 고정카드 4개 모두의 제목이 대표 사례).
2. **그 값을 풀어서 설명하는 서술(본문) → AI.** 조합의 경우의 수가 너무 많아 사람이 다 써둘 수 없는 경우.
3. **"제목"인데 그 자체가 개인화된 재미 포인트인 경우(Zone4 가변 카드 제목처럼) → AI 자유 생성 + 톤 참고 예시.** 후보 문구를 "고를 수 있는 선택지"로 주면 AI가 그대로 베껴 쓴다(실제 발생한 문제) — 예시는 톤/형식 참고용으로만 주고, 그대로/살짝 변형 사용은 명시적으로 금지해야 진짜 새 문장이 나온다. ⚠️ Zone4 **고정** 카드(오프닝 3개 + 클로징 1개)의 제목은 이 규칙 대상이 아니다 — "항상 등장하는 구조"라 규칙 1번(룰베이스)을 따른다. 이 규칙은 AI가 주제를 직접 고르는 가변 카드에만 해당.
4. 구현하면서 위 기준으로 봤을 때 AI보다 룰베이스가 맞다고 판단되는 지점이 나오면, 이 문서에 있는 "AI 생성" 표시와 다르더라도 룰베이스로 바꾼다. (구현 중 발견되는 예외는 이 문서를 그때그때 갱신)

## 3. AI 생성 지침

**Zone4 가변 카드 제목 (AI 자유 생성 — 아래 예시는 "톤 참고용"일 뿐, 후보 목록이 아니다. 고정 카드(오프닝 3개+클로징 1개)에는 적용 안 됨)**
- 아래 말투 예시는 절대 그대로 쓰거나 단어만 바꿔 쓰지 말 것 — 반드시 그 카드의 실제 풀이 내용에서 나온 표현으로 매번 새로 지을 것(2026-08-21: 후보뱅크로 줬다가 AI가 그대로 베낀 문제 발견 후 수정)
- 2030 타깃, MBTI 풀이처럼 이해가 쉽게
- **전문용어 사용 금지** — 현침살, 자충수, 천정이 밝다, 재백궁 등 사주/관상 전문 용어를 그대로 노출하지 않고 풀어서 표현
- **자음 표현(ㄴㄴ, ㄱㄱ 등) 사용 자체를 금지**
- **문장 끝에 짧은 해시태그 1~2개를 붙일 것(2026-08-22 9차 개편)** — 경쟁 사주 앱("이게사주")이 `#관살혼잡` `#고란살`처럼 제목 끝에 해시태그를 붙여 캡처·공유하고 싶게 만드는 걸 보고 벤치마킹했다. 단, 우리는 전문용어 노출 금지 규칙이 있으므로 해시태그도 원어 그대로 쓰지 않고 이 앱 톤(위트 있는 신조어풍)으로 새로 짓는다 — 예: `#K사장님각` `#무재사주주의` `#낯가림인싸`. 참고 예시의 해시태그도 그대로 베끼면 안 됨.
- 글자 수 기준은 아직 정하지 않음 — 실제로 생성해보고 너무 길면 추후 기준을 추가(해시태그 포함해서 화면 두 줄 이내)
- 단어 선택/뉘앙스/말투 예시 (아래는 참고용, 위 규칙이 항상 우선):
  - 누가 뭐래도 당신은 모두가 기댈 수 있는 든든한 산 #든든가장상 #의지대상1순위
  - 재물 바다를 품은 거대한 태산, 스케일이 다른 K-여사장님 재질 #사업가마인드 #스케일깡패
  - 알잘딱깔센 재테크의 신, 근데 통제는 살짝 힘들어함 #저축실패요정 #플렉스주의
  - 사장님 할 거 아니면 답답해서 어찌 사시나? 팩폭 들어갑니다 #보스기질 #지시받기싫어
  - 돈 복은 타고났네, 머니 파이프라인 심는 재능 만렙 #부자될상 #재테크만렙
  - 연애는 프리스타일, 결혼은 비혼주의? No, 숨은 인연 찾기 #인연은있다 #밀당고수
  - 아버지는 태평양, 어머니는 등대... 나는 그 사이의 섬 #가족애틋 #중간자포지션
  - 서울보다는 강릉, 해외는 캐나다? 따스한 햇살과 숲이 필요해 #힐링필요 #자연인기질
  - 애증의 K-가족, 그래도 내 편인 거 알지? #가족애증 #그래도내편
  - 인싸인 듯 아싸, 아싸인 듯 인싸 #낯가림인싸 #혼자놀기장인
  - 역마살? ㄴㄴ 글로벌 인재임. 해외로 튀어! (※ 이 예시의 "ㄴㄴ" 자체는 위 자음 표현 금지 규칙에 걸리므로 실제 생성 시 제외)

**Zone4 고정 카드 (나의 기질 / 남이 모르는 내 모습 / 조언) 작성 지침**
- 제목은 위에서 정한 고정 문구를 그대로 쓴다(AI가 짓지 않음).
- reading은 3~5문장, 위 가변 카드와 같은 2030 MBTI 톤 유지, 전문용어·자음 표현 금지 규칙도 동일하게 적용.
- "나의 기질"은 반드시 오행 개수(zero/과다 여부)를 먼저 짚고, 신강/신약·용신을 그 다음 근거로 자연스럽게 엮을 것 — 셋을 따로따로 나열하지 말 것.
- "남이 모르는 내 모습"은 관상과 사주가 실제로 다른 얘기를 할 때만 의미가 있다 — 둘이 같은 얘기만 하면 억지로 반전을 만들어내지 말고, 그 경우엔 "관상과 사주가 일치해서 겉과 속이 같은 사람"이라는 방향으로 풀어도 된다.
- "조언"은 진단이 아니라 방향 제시 — "당신은 ~한 사람이에요" 대신 "그래서 지금은 ~하면 좋아요"로 마무리(캐릭터 카드 작성 규칙과 동일 원칙).

**Zone4 가변 카드 분리 규칙**
- 후보 주제 6개(가족/일/돈/사랑/대인관계/쉼힐링)는 전부 최소 1개 카드로 나온다 — "이 사람에게 안 맞는 주제라 아예 뺀다"는 없음.
- **family(가족)·love(사랑)는 반드시 2개 이상** — 가족은 "자라온 환경 전반" + "부모님과의 관계"(십성 재성/인성 근거), 사랑은 "매력·인기"(신살+관상 눈 유형 근거, 먼저) + "연애·결혼 스타일"(나중)로 고정 분리.
- 나머지 주제(일/돈/대인관계/쉼힐링)도 내용이 많으면 최대 3개까지 세부 카드로 분리할 수 있다. 예: 돈 풀이에서 사업가 기질이 강하게 나오면 "돈"과 "사업"을 별도 카드로.
- 전체 가변 카드 수는 **최소 8개**(가족2 + 사랑2 + 나머지 4주제 각 1개 이상).
- 최종 카드 개수 = 고정 항목 4개 + 가변 항목(주제 6개, 총 8~18개) = **최소 12개**.

**Zone4 이유(basis) 영역**
- 각 카드 풀이(reading) 아래에 "왜 이렇게 풀이했나요?" 영역을 두고, 어떤 사주x관상 데이터 근거로 이렇게 풀이했는지 서술한다 (궁합보기 화면의 `basis` 필드와 동일 패턴). 오프닝 고정카드 2~3, 클로징 고정카드(조언)도 동일하게 적용(카드1 "인생의 흐름"만 예외).

## 4. 사주x관상 근거 확장 — 신규 연결 데이터 (2026-08-21 3차 개편)

### 0. 배경

Zone4가 6개 주제를 다뤘지만 근거가 오행 분포·신살 나열 위주로 얕았다. DB를 다시 훑어보니 **이미 계산은 되지만 개인 리포트(Zone4)엔 한 번도 전달되지 않던 사주 엔진**이 있었다 — 십성(十星)·신강신약·용신([js/app.js:1003-1144](js/app.js:1003)). 지금까지는 궁합보기 Zone2(`buildSipseongCross`/`buildYongsinChemi` — 두 사람 비교용)에만 쓰였다. 이번 개편은 이 엔진을 개인 리포트에도 연결하고, 신살·귀인 40여 종([js/app.js:1422](js/app.js:1422))을 "조합"으로 풀이하도록 프롬프트를 강화하는 작업이다.

**새로 프롬프트에 들어간 데이터 블록** (`buildDeepReportUserPrompt` 내부):
| 블록 | 값 | 의미 |
|---|---|---|
| `[십성 목록]` | `calcSipseongAll(pillars)` → `{year, month, hour}` (예: `{year:'편재', month:'정인', hour:'식신'}`) | 일간 대비 년간·월간·시간의 십성. 재성(편재·정재)=재물/아버지 인연, 인성(편인·정인)=학문·보살핌/어머니 인연, 식상(식신·상관)=표현력·언변, 관성(편관·정관)=조직·카리스마 |
| `[신강/신약]` | `calcSinkangSinyak(pillars)` → `{isStrong, help, drain, dayRooted}` | 일간을 돕는 힘(비겁·인성) vs 소모시키는 힘(식상·재성·관성)의 균형. 신강=스스로 밀고 나가는 힘, 신약=주변 도움이 필요한 구조 |
| `[용신(필요 오행)]` | `calcYongsin(pillars)` → `{isStrong, yongsinOh, ohCount, ...}` | 신강신약까지 고려해서 "이 사람에게 실제로 부족해서 채우면 좋은 오행" 하나를 고른 값. 오행 분포 그래프의 %보다 한 단계 더 정제된 결핍 지표 |
| `[오행 비교표]` | 오행별 `{사주퍼센트, 관상퍼센트, 차이_관상마이너스사주, 더높은쪽}` | 사주 오행(개수, 0~8)과 관상 오행(퍼센트, 0~100)을 같은 % 스케일로 맞춰서 오행별로 어느 쪽이 더 높은지 미리 계산해둔 표 |

이 값들은 AI가 만들어내는 게 아니라 **JS에서 미리 계산해서 프롬프트에 문자열로 박아 넣는다** — AI는 그 값이 왜 그런지 설명하는 문장만 쓰고, 값 자체를 새로 추정하거나 다르게 계산하지 않는다(§2 판단 기준 1번과 동일 원칙). 지금까지는 이 정보 자체가 AI에게 전달되지 않아서 "나의 기질"·가족·재물·일 카드에서 이 근거를 아예 쓸 수 없었다.

**⚠️ 2026-08-21 버그 수정 — 오행 비교 방향 오류**: `zone3_ohaeng_reading`이 실제 화면 그래프와 반대되는 방향으로 서술하는 버그가 있었다(예: 화면엔 관상 목=19% > 사주 목=13%인데, "관상 쪽 목 기운이 낮게 나타난다"고 씀). 원인은 `[사주 오행 분포]`(사주 8글자 중 개수, 0~8)와 `[관상 오행 분포]`(`calcFaceOhaeng`, 이미 0~100 퍼센트)가 서로 다른 스케일로 그대로 프롬프트에 들어가 있어서, AI가 두 숫자를 직접 비교하다 방향을 헷갈린 것. `[오행 비교표]`를 새로 추가해 사주 쪽도 퍼센트로 정규화하고 오행별 "더높은쪽"까지 코드로 미리 계산해서 주도록 고쳤다 — AI는 이제 이 표의 방향만 그대로 따르면 된다.

### 1. 테마별 매핑 — 최종 배치

| 예시 테마 | 배치 (Zone4 내) | 사주 근거 | 관상 근거 |
|---|---|---|---|
| 개인 기질(오행 zero/과다 비유) | **고정카드2 "나의 기질"** | 오행 개수 + `[신강/신약]` + `[용신]` | 관상 오행분포·6기질 |
| 남이 모르는 내 모습(반전) | **고정카드3 "남이 모르는 내 모습"** | 오행·십성(편인/정인 등) | 전체 인상 vs 눈매·부위 |
| 마무리 조언 | **클로징 고정카드 "조언" (Zone4 맨 마지막)** | Zone3·Zone4 전체 데이터 종합(십성·신살조합·용신 등) | 관상 전체 근거 종합 |
| 자라온 환경 | **family 카드1** | 오행/십성 전반 | 관상 6기질 |
| 부모님과의 관계 | **family 카드2 (신규)** | `[십성 목록]`의 재성(아버지)·인성(어머니) 유무 | — (십성은 관상 대응 데이터가 없어 사주로만 풀이) |
| 인기·매력(이성운) | **love 카드1 (신규, 순서상 첫 번째)** | 홍염살·년살(도화 계열) | 도화안(EYE_PEACH)·난안(EYE_LUAN) 등 매력 계열 눈 유형([js/archetype-db.js:18](js/archetype-db.js:18)) |
| 연애·결혼 스타일 | **love 카드2 (기존, 순서상 두 번째)** | 일간·오행 | 관상 6기질 |
| 재물 | **money (보강)** | `[십성 목록]`의 재성(편재=사업감각·정재=성실 축적) | 코끝(nosetip)·입(mouth) — 이미 "재물운" 태그 있음([js/character/face-trait-map.js:142](js/character/face-trait-map.js:142)) |
| 직업 적성 | **work (보강)** | `[십성 목록]` 조합(상관=언변, 편관=카리스마, 편인=독창성 등) | Zone1 캐릭터의 `work` 필드와 중복되지 않게, 십성으로 더 구체화 |
| 대인관계 | **relationships (보강)** | 화개살(혼자 시간)·귀문관살(몰입)·겁살/재살(마찰) | 눈썹·미간 — 이미 "대인관계·형제운" 태그 있음([js/character/face-trait-map.js:138-139](js/character/face-trait-map.js:138)) |
| 쉼/힐링 | **rest (신규 근거)** | `[용신]`의 부족 오행을 채우는 활동·공간 제안 | — |

(⚠️ 2026-08-21 4차 수정: "개인 기질"과 "남이 모르는 내 모습"은 처음엔 각각 Zone3 보강·Zone1 `personality_detail` 흡수로 설계했는데, 사용자 피드백으로 "개인적인 설명이 실제로 안 보인다"는 문제가 확인돼 Zone4 전용 고정 카드로 독립시켰다. "조언" 고정카드도 이번에 신규 추가(2026-08-21 5차 개편에서 Zone4 맨 마지막으로 위치 이동).)

### 2. Zone4 전체 노출 순서 (최종)

```
[오프닝 고정] 인생의 흐름 → 나의 기질 → 남이 모르는 내 모습
[가변] family(자라온 환경 → 부모님과의 관계) → relationships → love(매력·인기 → 연애·결혼 스타일) → work → money → rest
[클로징 고정] 조언 (2026-08-21 5차 개편 — 맨 마지막으로 이동)
```

가변 카드 쪽 topic 순서(`family → relationships → love → work → money → rest`)는 렌더 시점에 고정 정렬한다(`ZONE4_TOPIC_ORDER`, AI가 반환하는 배열 순서에 맡기지 않음). "어디서 왔는지(가족) → 사람을 어떻게 대하는지(대인관계·사랑) → 무얼 하며 사는지(일·돈) → 어떻게 쉬는지(쉼/힐링)"로 읽히는 순서를 의도했다. 같은 topic_key 카드끼리(family 카드1·2, love 카드1·2)는 프롬프트 지시로 순서를 못박아 AI가 그 순서대로 배열하게 한다.

### 3. 신살·귀인 "조합" 해석 지시

기존엔 신살·귀인을 "실제로 있는 것만 언급하라"는 지시만 있고, 여러 개를 어떻게 엮을지는 지시가 없어서 나열식으로만 나왔다. 시스템 지침에 아래 원칙을 추가한다:

- 2개 이상의 신살·귀인이 있으면 따로따로 설명하지 말고, 같은 방향을 가리키는 조합을 찾아 "하나의 이야기"로 묶어서 풀이할 것 (예: 홍염살+년살 → 각각 설명 대신 "가만히 있어도 시선을 끄는 사람"으로 통합)
- 같은 방향 조합이 없으면 가장 뚜렷한 신살·귀인 1개만 써도 됨
- 목록에 없는 신살·귀인을 조합해서 지어내는 것은 여전히 금지

이 원칙은 시스템 지침의 "[사주]" 섹션에 있어서 `zone4_cards`뿐 아니라 아래 §5의 `sinsal_reading`(신살 종합 풀이)에도 그대로 적용된다.

### 4. 십성 → family/love 카드 연결

`zone4_cards` 스키마의 `[주제별 근거 가이드]`에 family 카드2("부모님과의 관계") 작성 규칙을 명시:
- `[십성 목록]`(year/month/hour)에 편재·정재(재성)가 하나라도 있으면 아버지와의 인연이 뚜렷함을, 3자리 모두 없으면 인연이 약하거나 독립적으로 형성됐음을 풀이
- 편인·정인(인성)은 같은 방식으로 어머니와의 인연에 적용
- 실제 가족 구성원의 생사·건강 등을 예언·평가하지 말고 "성향에 미쳤을 습관·태도 차이" 중심으로만 서술(기존 family_background 금지 규칙과 동일선상)

같은 방식으로 love 카드1("매력·인기")도 신살(홍염살·년살) + 관상(도화안 등 매력 계열 눈 유형)을 반드시 근거로 쓰도록 명시했고, money/work/relationships/rest도 각각 십성·신살·관상 부위·용신을 지정된 근거로 쓰도록 `[주제별 근거 가이드]`에 못박았다(위 §4-1 표 참고, 상세 문구는 스키마 원문 참고).

### 5. 버려지던 필드 재연결 — 부위별 관상 풀이 · 신살 종합 풀이 (2026-08-21 5차 개편)

`growth_guidance`("조언" 카드로 재활용, §1 참고)와 같은 패턴의 문제가 두 곳 더 있었다 — 관상탭·사주탭에서 이미 AI에게 요청·생성되는 필드인데 통합분석엔 담을 그릇이 없어서 매번 버려지고 있었다.

| 필드 | 배치 | 렌더 함수 |
|---|---|---|
| `part_deep_dive` (관상 부위별 4~6개 상세해설) | Zone3 페어1.5 "🔮 관상 정보"(사주 원국 아래, 대운x삼정 위 — 2026-08-21 6차 개편으로 Zone1에서 이동. 오행능력치 비교가 8차 개편으로 Zone2로 옮겨가면서 사주 원국 바로 다음 자리가 됐다) | `renderPartDeepDive` — 관상탭의 `partDeepDiveCardHtml`을 그대로 재사용 |
| `sinsal_reading` (신살·귀인 종합 해석) | Zone3 페어1 "사주 원국" 표 바로 아래 | `renderReadingBasis` — reading 아래에 basis를 "왜 이렇게 풀이했나요?"로 붙임 |

- `sinsal_reading`은 이제 "몇 개나 있는지·다양성 자체의 의미"를 먼저 짚고 그 다음 조합 해석으로 들어가도록 필드 설명을 강화했다(기존엔 그냥 "3~5문장 해석"이라 목록 나열에 그치기 쉬웠음).
- `part_deep_dive`는 필드 설명을 바꾸지 않았다 — 관상탭과 완전히 같은 내용·같은 카드 UI로, 통합분석에서도 "눈이 작다·코가 좋다 같은 실측 근거로 왜 이렇게 해석되는지"를 그대로 보여주기만 하면 됐다. 단, **7차 개편(2026-08-21)에서 `reality_tip`(현실 조언)을 완전히 제거**했고(관상탭 포함 전역 — 스키마·`partDeepDiveCardHtml` 양쪽에서 삭제), 카드 안 근거 박스(관상 분석/전통 관상 원리) 배경도 `rgba(0,0,0,0.12)`에서 `#fff`로 바꿨다(이 앱은 다크모드가 없는 고정 라이트 테마라 안전한 변경).

### 6. 풀이-근거 분리 확장 — sinsal_reading·zone3_manseryeok_reading (2026-08-21 7차 개편)

실측 사례에서 `sinsal_reading`·`zone3_manseryeok_reading`이 "다양한 기운들이 조화롭게 섞여 있다" 식으로 뭉뚱그려 나오는 문제가 확인됐다(사용자 피드백: "내 사주 원국이 뭔데? 어떤 살풀이를 이렇게 푼 건데?" — 근거가 안 보인다). Zone4 가변카드의 `reading`+`basis` 패턴을 이 둘에도 적용했다.

- **`sinsal_basis`**(신규) — `sinsal_reading` 바로 아래. 일간 오행, 어느 기둥(년/월/일/시주)인지, 실제 신살·귀인 명칭을 그대로 인용하도록 필드 설명에 못박음. "다양한 기운" 같은 추상 표현만 쓰는 것은 금지.
- **`zone3_manseryeok_basis`**(신규) — `zone3_manseryeok_reading` 바로 아래. 일간 오행과 관상 6기질 중 실제로 높게/낮게 나온 기질명·점수를 그대로 인용.
- `sinsal_reading`·`zone3_manseryeok_reading` 자체의 설명도 "reading 문장 안에서도 실제 근거 이름을 최소 1~2개 언급할 것"으로 강화 — basis에만 근거를 미루지 않고 이중으로 구체성을 강제.
- 렌더는 `renderReadingBasis(elId, reading, basis)` 신규 함수 — Zone4 가변카드의 `gg-item-basis` 스타일을 그대로 재사용해 "왜 이렇게 풀이했나요?"로 노출.
- `zone3_ohaeng_reading`·`zone3_daeun_reading`은 이번엔 그대로 뒀다(사용자가 사주원국 파트만 먼저 확정) — 같은 패턴으로 확장할지는 추후 판단.

### 7. UI 버그 수정 (2026-08-21 7차 개편)

- **관상 정보 카드 여백 축소** — `partDeepDiveCardHtml`([js/ai-analysis.js:1281](js/ai-analysis.js:1281)) 바깥 카드의 `padding:18px 16px`에서 좌우 16px를 없애고 `padding:18px 0`으로 바꿨다(사용자 피드백: 공간을 너무 많이 차지함).
- **오행능력치 비교 그래프 막대 길이 버그** — `renderOhaengCompareTable`([js/app.js:1865](js/app.js:1865))이 막대 길이를 "5개 오행 중 최댓값(maxPct)에 대한 상대값"으로 그리고 있었다. 그래서 예를 들어 관상 화(火)가 34%인데도 그 34%가 그 세트 안에서 제일 큰 값이면 막대가 꽉 찬 100%처럼 보였다 — 바로 옆에 적힌 숫자(34%)는 진짜 값인데 막대만 상대값이라 서로 안 맞았다(사용자 리포트: "34%인데 왜 꽉 찼어? 그래프 표기가 이상한데"). `width`를 `facePct`/`sajuPct` 값 그대로(최대 100% 캡) 쓰도록 고쳐서, 막대 길이가 숫자와 항상 일치하게 만들었다.

## 8. Zone2 개편 — 같은 점/다른 점 신설 + 오행 에너지 비교 이동 (2026-08-22 8차 개편)

### 배경

사용자 피드백 두 가지를 한 번에 반영했다:
1. 케미 점수(당시 "관상x사주 케미 점수", 단위 "점")가 뭘 재는 값인지 직관적이지 않다 — "관상·사주 싱크"로 이름을 바꾸고 단위를 "%"로 바꿨다(계산식은 `computeGwansangSajuChemi` 그대로, 표기만 변경). 기운 줄다리기 바(관상 65%·사주 35% 같은 값)도 뭘 뜻하는지 설명이 부족했다 — 고정 캡션을 하나 더 추가했다.
2. Zone2가 "총평 한 문단 + 점수"뿐이라 "그래서 구체적으로 뭐가 같고 뭐가 다른지"가 안 보인다 — 같은 점/다른 점 불릿과, Zone3에 있던 오행 에너지 비교 그래프를 Zone2로 끌어올려 "총평 자리에서 바로 근거까지" 보이게 했다.

### Zone2 구성 순서 (8차 개편 당시 — ⚠️ 최종 총평 위치는 9차 개편에서 다시 바뀌었다, §9 참고)

```
헤드 문구 → 케미 점수(관상·사주 싱크, %) + 기운 줄다리기 바
→ 둘이 같은 점 · 다른 점 (신규)
→ 오행 에너지 비교 차트 (Zone3에서 이동, 그래프+융합풀이 통째로)
→ 최종 통합 총평 (기존 zone2_review — 내용 그대로, 위치만 맨 아래로)
```

### 같은 점 / 다른 점 — 신규 필드

- **`zone2_common_points`**(문자열 배열, 2~3개) — 관상과 사주 모두에서 상대적으로 높게 나온 기질을 짧은 구문으로. 예: "실행력이 강함", "책임감/완수력이 강함".
- **`zone2_different_points`**(객체 배열, 1~2개, 각 `{face, saju}`) — 관상 6기질과 사주 6기질의 순위가 엇갈리는 지점을 관상 쪽 표현(`face`)과 사주 쪽 표현(`saju`)으로 대비. 예: `{face: "밖으로 밀어붙이는 힘", saju: "안에서 신중하게 판단하는 힘"}`.
- 두 필드 모두 **AI 생성** — §2 판단기준 2번(경우의 수가 많아 사람이 다 써둘 수 없는 서술) 적용. 다만 근거 숫자 자체는 새로 만들지 않는다 — 아래 데이터 블록을 그대로 인용만 하도록 스키마에 못박았다.
- **새 데이터 블록 `[사주 6기질 점수]`** — 기존엔 `[관상 6기질 점수]`(`faceTraitScores`)만 프롬프트에 넘기고 있어 AI가 사주 쪽 기질 순위를 추측해야 했다. `zone1Character.sajuRaw`에 `computeTraitScoresFromRaw`+`SAJU_TRAIT_BASELINE`을 똑같이 적용해([js/ai-analysis.js:1864](js/ai-analysis.js:1864) 부근 `zone3Extra.sajuTraitScores`) 관상과 같은 0~100 스케일로 맞춰 넘긴다 — 두 블록을 직접 비교해서 "같은 점"(둘 다 높은 기질)·"다른 점"(순위가 엇갈리는 기질)을 뽑을 수 있게 됐다.
- 렌더는 `renderZone2CommonDiff(elId, common, different)`([js/ai-analysis.js](js/ai-analysis.js)) — 같은 점은 캐릭터 상세의 `char-detail-list.is-strength`(✓ 아이콘)를 재사용, 다른 점은 화면 전체의 관상=jade/사주=beige 배색 규칙(`dom-bar-labels`·`oh-headline-split`과 동일)을 그대로 따라 `.cmb-diff-row .face`/`.saju`로 표시.

### 오행 에너지 비교 이동

- Zone3 페어2였던 "🌳 오행능력치 비교"(그래프 + `zone3_ohaeng_reading` 융합풀이)를 통째로 Zone2로 옮겼다 — 그래프만 옮기고 풀이를 Zone3에 남기면 그래프와 설명이 서로 다른 Zone에 떨어져 있어 어색하다고 판단, 한 덩어리로 이동시켰다.
- 제목을 "오행능력치 비교"에서 "🌳 오행 에너지 비교"로 다듬었다(그래프+풀이가 Zone2 총평의 일부가 되었으니 "능력치"보다 "에너지"가 총평 톤에 더 맞음).
- 컨테이너 id는 `cmbZone3Reading2` → `cmbZone2OhaengReading`으로 정리했다(Zone을 옮겼는데 id에 "Zone3"가 남아있으면 나중에 혼란을 준다). `data.zone3_ohaeng_reading`(AI 응답 필드명 자체)은 바꾸지 않았다 — 스키마·프롬프트를 건드릴 이유가 없는 내부 데이터명이라 UI 이동과 분리했다.
- Zone3에는 이제 페어1(사주 원국)·페어1.5(관상 정보)·페어2(대운x삼정, 구 페어3)만 남는다.

### 로딩 스켈레톤

`AI_ZONE_SKELETON`([js/app.js:2028](js/app.js:2028))에서 `cmbZone2CommonDiff`·`cmbZone2OhaengReading` 모두 `cmbZone2`(자기 자신)로 매핑을 바꿨다 — `cmbZone2OhaengReading`은 원래 `cmbZone3`에 매핑돼 있었는데, Zone을 옮겼으니 로딩 중 "불러오는 중" 표시도 옮겨간 Zone(cmbZone2)에 붙어야 한다.

### 버튼 문구 변경

인연도감에서 통합분석으로 넘어가는 CTA 버튼 문구를 "관상 + 사주로 다시 보기" → "관상에 사주까지 더해 깊게 보기"로 바꿨다([js/inyeon-dogam.js:796](js/inyeon-dogam.js:796)) — "다시 보기"가 이미 본 걸 또 본다는 뉘앙스라, 관상 위에 사주를 더해서 더 깊이 들어간다는 의미가 잘 안 살았다는 사용자 피드백.

## 9. Zone2 최종 총평 원위치 + Zone4 제목 해시태그화 (2026-08-22 9차 개편)

### 배경

경쟁 사주 앱("이게사주")과 비교해본 뒤 나온 후속 조정 두 가지.

### 최종 총평 위치 원복

8차 개편에서 최종 총평(`zone2_review`)을 Zone2 맨 아래(같은점/다른점·오행 비교 다음)로 옮겼는데, "케미 점수 밑에 바로 연결해서 보고 싶다"는 요청으로 다시 케미 점수 카드 안(기운 줄다리기 바 바로 아래)에 붙였다 — 8차 개편 이전 원래 구조로 복귀. `zone2_review` 필드·`renderZone2Review` 함수는 손대지 않았고 렌더 대상 컨테이너(`cmbZone2Review`)의 HTML 위치만 옮겼다. 같은 점/다른 점, 오행 에너지 비교는 8차 개편 그대로 Zone2에 유지.

기운 줄다리기 바(관상/사주 %) 위에 있던 고정 캡션("둘 중 어느 쪽 기운이 더 강하게 드러나는지를...")도 이번에 삭제 — 사용자가 불필요하다고 판단.

### Zone4 가변 카드 제목 — 해시태그 형식 추가

경쟁 앱이 카드 제목 끝에 `#관살혼잡` `#고란살`처럼 해시태그를 붙여 캡처·공유 욕구를 자극하는 걸 보고, `zone4_cards.title`에도 같은 형식을 추가했다. 다만 이 앱은 전문용어 노출 금지 원칙이 있어 원어 그대로 쓰지 않고 이 앱 톤(위트 있는 신조어풍)으로 새로 짓게 했다 — 예: `#K사장님각` `#무재사주주의`.

- 스키마 `title` 필드 설명([js/ai-analysis.js](js/ai-analysis.js) `zone4_cards.items.title`)에 해시태그 1~2개 요구 추가.
- 시스템 지침 `[Zone4 제목·풀이 말투]` 블록에도 동일 규칙 추가.
- `CMB_ZONE4_TITLE_STYLE_EXAMPLES`(말투 참고 예시 10개) 전부 끝에 해시태그를 붙인 형태로 갱신 — 위 §3 예시 목록과 동기화.
- 고정 카드(오프닝 3개+클로징 1개) 제목은 이번에도 대상이 아니다 — §2 판단기준 1번(룰베이스 고정 문구) 그대로 유지.

## 10. Zone4 "고민 해결" 카드 신설 — 진입 질문 3번(자유 입력) 반영 (2026-08-27, 구현 완료)

### 배경

진입 질문 3개(1.지금의 상황/2.일상/3.자유 입력) 중 q1·q2는 `buildLoveCardGuidance`/`buildWorkCardGuidance`([js/ai-analysis.js:478](js/ai-analysis.js:478))가 love/work 카드 각도를 스키마 설명에 직접 박아 넣어 반영을 강제하는데, q3(자유 입력)는 `situationBlock`([js/ai-analysis.js:1312](js/ai-analysis.js:1312))에 참고 텍스트로만 끼어들고 전용 출력 필드가 없어 반영 여부가 AI 재량에 맡겨져 있었다(사용자 리포트: "3번은 반영 안 되는 것 같다" — 코드 확인 결과 사실로 확인됨).

### 카드 스펙

- **위치**: 가변 카드의 `topic_key`를 `ZONE4_TOPIC_ORDER`(family/relationships/love/work/money/rest)에 없는 값(예: `user_question`)으로 주면, 기존 정렬 로직(`renderZone4Cards`, "목록에 없는 topic_key는 맨 뒤로 보낸다")에 의해 **자동으로 가변 카드 중 맨 마지막**에 위치한다 — 즉 클로징 고정 카드("조언") 바로 위. 별도 순서 코드 변경 불필요.
- **트리거**: q3에 답변이 있을 때만 생성. 미답변이면 카드 자체를 만들지 않는다(가짜 고민 지어내기 금지).
- **제목**: 고정 패턴 `"OO(이름)의 질문, 냥반이 답해드려요"`(2026-08-27 문구 확정 — 최초 초안은 "OO님의 고민, 해결해드릴게요"였음) — 가변 카드지만 이 카드만 제목 형식이 고정(§2 판단기준 3번 예외 — 이름만 채워 넣는 템플릿이라 AI 자유 생성 대상이 아님). 실제 title 필드는 AI가 뭘 보내든 `buildQ3CardTitle`([js/ai-analysis.js:537](js/ai-analysis.js:537))이 코드로 덮어쓴다.
- **주제 판별**: 키워드 사전 매칭을 1차로 시도(이사→이동수, 짝사랑→연애운 등) → 매칭 실패 시 질문 원문을 스키마에 그대로 강제 주입하고 "반드시 이 질문에 직접 답하라"는 지시로 폴백.
- 건강 · 임신/자녀 · 소송처럼 키워드는 잡히지만 진단·법적 판단으로 오인될 수 있는 주제는 "단정적 결과 대신 흐름/시기 관점으로만 답하고, 필요하면 전문가 상담을 권하라"는 별도 가드 문구를 프롬프트에 추가한다.
- **reading 작성 규칙은 §3 "전문용어 사용 금지" 규칙을 그대로 따른다** — 현침살·정관·신강 같은 전문 용어를 `reading`에 노출하지 않고 풀어서 쓴다. 전문 용어는 `basis`("왜 이렇게 풀이했나요?") 안에서만 쓴다. (2026-08-27 사용자 지적: 최초 목업 예시 문장에 "신강한 일간", "정관과 부딪히는 해" 같은 표현이 그대로 들어가 있어 이 기존 규칙 위반 — 목업 수정하며 발견)

### 목업

[scratch/zone4-question-card-mockup.html](scratch/zone4-question-card-mockup.html) — 이사/짝사랑/이직운/키워드매칭실패(폴백) 4가지 예시 카드 + 확장 가능한 질문 케이스 표(일반 8개 + 신중 주제 3개).

### 구현 반영

- [x] 키워드 사전 `CMB_Q3_TOPIC_DICT`/`matchQ3Topic`([js/ai-analysis.js:504](js/ai-analysis.js:504)) — 일반 11개 + 신중 주제 3개(건강/임신·자녀/소송)
- [x] `zone4_cards` 스키마에 `topic_key: user_question` 추가, `buildQ3CardGuidance`가 카드 생성 여부·주제·가드 문구를 조립해 스키마 설명에 연결([js/ai-analysis.js:499](js/ai-analysis.js:499))
- [x] 제목은 AI 생성이 아니라 `buildQ3CardTitle`이 렌더 직전에 코드로 강제 — 이름은 `state.combined.name`(신규, `applyToContext`가 프로필에서 채움, [js/profile.js:286](js/profile.js:286))
- [x] q3 미답변인데 카드가 왔을 때 걸러내는 안전장치를 `requestDeepReport` 호출부에 추가([js/ai-analysis.js:2123](js/ai-analysis.js:2123))
- [x] 건강/임신·자녀/소송 가드 문구 — `buildQ3CardGuidance`의 caution 분기

### 다음 단계 (미착수)

- [ ] 실제 Gemini 응답으로 여러 케이스(이사/짝사랑/이직/폴백/신중 주제) 검증 — 로컬 콘솔 스크립트로 buildCombinedReport를 직접 호출해 확인 가능(로그인·냥 없이 실제 API 호출)
- [ ] 키워드 사전이 커버 못 하는 실사용 질문이 쌓이면 사전 확장
