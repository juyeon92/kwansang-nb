---
문서: 인연도감 서비스 정책 (안)
적용범위: js/inyeon-dogam.js, functions/index.js, functions/engine/compatibility-engine.js, firestore.rules
상태: 정책 확정(v4) — 세부 UI 값(케미 필터 칩 구간 등)·필수 동의 절차만 후속 확정 예정
참고: 로그인 유도 UX는 사주도령 귀인지도 레퍼런스(대화 중 첨부된 스크린샷 2장 — ①생성 직후 모달 ②지도 화면 내 지속 배너)를 따른다
최종수정: 2026-09-03
---

## 0. 배경

인연도감은 "A가 본인 관상 결과를 지인에게 공유 → 지인이 등록 → A가 지인과의 케미 점수를 확인"하는 기능이다.
v1 초안 이후 실제 화면 흐름을 검토하며 다음을 확정했다:

- B가 본인 인연도감을 만드는 시점은 **명시적 액션 + 로그인 필수**로 게이트한다(자동 생성 아님)
- 로그인 시 계정에 이미 다른 캐릭터의 도감이 있으면, 사주도령 귀인지도 방식(대표 도감 지정 + 비대표 도감 접어서 보관)을 따른다
- 케미 점수 알고리즘 자체는 이번 범위 밖(현행 `compatScore()` 유지)

## 1. 용어

- **오너(A)**: 인연도감을 처음 만들고 공유 링크를 배포하는 사람
- **게스트**: 공유 링크를 받아 들어온 사람(B, C, D...) — 등록 후에는 "참여자"로 부른다
- **슬러그(slug)**: 도감 하나를 가리키는 추측 불가한 랜덤 키. 공유 링크(`?dogam=<slug>`)에 노출되는 유일한 식별자
- **엔트리(entry)**: 특정 도감에 등록된 참여자 1명의 기록(닉네임, 캐릭터, 케미 점수 등)
- **케미 점수**: 두 캐릭터 조합에 대해 계산되는 궁합 점수(현행 알고리즘 그대로 사용), 항상 **점수를 보는 사람 기준**으로 계산·저장(5장 참고)
- **대표 도감**: 한 계정이 여러 도감을 갖게 됐을 때(3장, 충돌 상황), 기본으로 노출되는 도감

## 2. 전체 플로우

### 2-0. 최초 진입 화면 (신규)

사진도 닉네임도 없는 완전 첫 방문 상태. 인연도감 기능 소개 문구 + 사진 업로드 + 닉네임 입력 + "관상 캐릭터 뽑기" 버튼으로 구성.

### 2-1. A — 최초 분석

1. 사이트 진입 → 인연도감 화면에서 **닉네임 입력 + 사진 업로드** → "관상 캐릭터 뽑기" 클릭
2. 랜드마크 기반 분석 실행 → 결과 화면으로 이동
3. 결과 화면 진입 시 **로그인 유도 모달**을 1회 노출("이 인연도감을 계정에 보관할까요?" + 로그인·회원가입하고 보관하기 / 나중에 할게요 — 사주도령 귀인지도 레퍼런스). 스킵해도 이후 도감 화면엔 **지속 인라인 배너**로 "회원가입 전 도감은 이 기기·브라우저에만 연결돼 있어요. 기기를 바꾸거나 브라우저 기록을 지우면 방장 권한과 보관함 연결을 잃을 수 있어요"를 로그인 전까지 계속 노출한다. 모달은 반복해서 띄우지 않고, 배너만 로그인 전까지 남아있는 방식(사주도령과 동일).
   - 로그인 안 함: 이 기기/브라우저에만 저장(익명 인증 uid + 로컬 저장소), 30일 후 자동 만료
   - 로그인 함: 계정에 귀속 저장, 만료 없음. 계정에 이미 다른 도감이 있으면 3장의 충돌 처리 적용. **로그인은 반드시 기존 익명 계정에 credential을 연동(link)하는 방식으로 처리해 uid를 그대로 유지한다** — 새 uid를 발급하면 이미 쌓인 entries의 uid 매칭이 끊어진다(6장 참고)
4. 결과 화면 구성: 내 캐릭터·특성 카드 + "내 인연도감" 카드 + 공유 링크 버튼(참여자 목록은 처음엔 빈 상태)

### 2-2. B — 공유 링크로 진입 및 등록

5. A의 공유 링크(`?dogam=<slug>`)로 진입 → 게스트 뷰. **상단에 A의 결과(캐릭터+특성)가 항상 고정 노출**
6. B가 본인 사진 업로드 + 닉네임 입력 + **"인연도감 등록하기"** 클릭 (기존 "관상 캐릭터 뽑기"에서 문구 변경 — A의 최초 버튼 문구와 구분)
7. 등록 처리 시 서버가 하는 일:
   - **A 기준** 케미 점수 계산: `compatScore(A캐릭터, B캐릭터)`
   - `dogam/{A slug}/entries/{B uid}`에 B 정보(캐릭터, 닉네임, A 기준 케미 점수) 저장
   - B 본인의 도감은 이 시점에 생성하지 않는다(2-4 참고)
8. B 결과 화면 구성 — 아래 두 영역만으로 구성 (v1의 "중단/하단"을 하나로 병합):
   - **상단**: A의 결과(고정, 6번과 동일 카드)
   - **2번째 영역(병합)**: B의 캐릭터 결과 + A와의 케미 점수 + "내 인연도감 만들기" CTA를 한 섹션 안에서 함께 노출("지금은 A님 도감에만 등록됐어요. 내 도감도 만들면 나만의 공유 링크가 생겨요" 안내 포함)
   - **3번째 영역**: A의 인연도감 — 지금까지 A의 링크로 연결된 전체 참여자 목록
   - **(v4) 이 화면은 등록 직후 1회성 확인 화면이다.** 같은 링크로 재방문하면 2-3의 게스트 공통 뷰(등록 폼)로 돌아간다 — 자기 결과를 다시 보고 싶으면 하단 참여자 목록에서 본인 항목을 탭해서 확인한다.

### 2-3. 공통 뷰 템플릿 — A의 링크로 들어오는 모든 진입

A의 슬러그로 들어오는 모든 진입은 방문자 유형에 따라 **상단은 항상 A의 결과로 고정**, 중단만 아래처럼 달라진다.

**(v4 변경) 게스트는 등록 이력과 무관하게 항상 동일한 화면을 본다.** 기기/세션 상태로 "이미 등록했던 사람"을 구분해서 다른 화면을 보여주지 않는다(사주도령 방식 채택, v1의 "재방문 참여자" 분기는 제거). 이유:
- 인연도감은 모임 자리에서 폰 하나로 여러 명이 순서대로 등록하는 상황이 흔한데, 기기 인식으로 "재방문 참여자" 화면을 띄우면 **다음 사람이 이전 사람의 케미 결과를 그대로 보게 되는 프라이버시 문제**가 생긴다
- 이미 확정한 "중복 등록 자유 허용"(4장) 정책과도 일관됨 — 애초에 동일인 식별을 안 하기로 했으니 재방문 판별도 하지 않는 게 일관적

| 방문자 유형 | 상단 | 중단 | 하단 |
|---|---|---|---|
| 게스트(신규든, 예전에 등록한 적 있든 구분 없음) | A의 결과(고정) | **등록 폼**(사진·닉네임 입력 + "인연도감 등록하기") — 항상 동일하게 노출 | A 인연도감 전체 참여자 목록 — 본인 항목을 탭하면 등록 당시 케미 상세를 다시 볼 수 있고, 근처에 "내 인연도감 만들기" CTA 상시 노출(재방문 시 자기 결과 재확인·도감 생성 유도를 이 목록이 대체) |
| 오너(A) 본인 | 내 결과+공유 링크 CTA | (등록 폼 없음 — 오너는 자기 도감에 등록할 수 없음) | A 인연도감 전체 참여자 목록 |

예외: **명시적으로 로그인한 상태**(익명 기기 인식이 아니라 실제 계정 인증)로 A의 링크에 들어온 B는 2-4의 "예외" 절차(이미 본인 도감이 있으면 프로필 재사용)를 그대로 적용한다 — 이건 기기 추정이 아니라 로그인이라는 명시적 신호에 기반하므로 위 프라이버시 우려와 무관하다.

참여자 목록(하단)은 오너/게스트 구분 없이 **누구나 전체 열람 가능**(4장), 케미 점수 순 정렬 + 관계 라벨 필터 칩(4장, 10장 참고).

### 2-4. B — "내 인연도감 만들기" (로그인 필수)

9. 이 버튼은 **로그인이 필수**다. 비로그인 상태에서 클릭하면 로그인 화면으로 유도하며, 이때도 **익명 계정 credential 연동 방식**으로 처리해 B가 A 도감에 등록할 때 쓴 uid를 그대로 유지한다(uid가 바뀌면 A 도감의 B 엔트리와 연결이 끊김).
10. 로그인 완료 시:
    - B가 **처음** 도감을 만드는 경우: B의 도감 문서 생성 + **B 기준** 케미 점수(`compatScore(B캐릭터, A캐릭터)`)로 A를 B의 도감에 자동 등록(B가 A를 다시 등록할 필요 없음)
    - B가 **이미 로그인 계정에 도감을 갖고 있는** 경우(예: 다른 친구 링크로 이미 만들어둔 상태): 사진 재업로드 없이 기존 프로필(닉네임+캐릭터, Firestore에 저장돼 있으므로 재사용 가능 — 사진 자체는 저장 안 하지만 캐릭터ID는 저장됨)을 그대로 보여주고, "인연도감 등록하기" 버튼 한 번으로 A↔B 양쪽에 동시 등록(A 도감엔 A기준 점수, B 도감엔 B기준 점수)
11. 이후 B도 본인 공유 링크를 받아 2-2~2-3과 동일한 사이클을 새로 시작할 수 있음

## 3. 로그인 시 계정 도감 충돌 처리 (사주도령 귀인지도 참조)

- 로그인했는데 계정에 **이미 도감이 있고**, 이번에 로그인 전/후로 만들어진 도감과 **캐릭터가 다른** 경우에만 아래 절차 적용(캐릭터가 같으면 자동 병합 — 참여자 목록만 합치고 캐릭터·점수 기준에 변화 없어 안전)
- **캐릭터가 다르면 병합하지 않는다.** 두 도감을 계정에 그대로 유지하고, 사주도령 귀인지도와 동일한 UX로 "대표 도감"을 지정한다:
  - "어느 걸 대표로 둘까요?" 화면에 각 도감(참여자 수 표시)을 라디오로 나열 → "대표로 정하기"
  - 대표 도감은 카드로 크게 노출(공유 링크 보내기 / 내 도감 보기)
  - 비대표 도감은 "친구가 있는 다른 도감 N개"로 접어서 아래에 보관, 필요하면 오너가 직접 **삭제**(9-5, 삭제 기능과 연동)해서 정리
  - 각 도감은 고유 링크·참여자 목록·점수를 그대로 유지 — 이미 연결된 게스트에게 영향 없음
- **범위 한정**: 이번엔 사주도령처럼 사용자가 언제든 "+새 도감 만들기"로 다중 도감을 만드는 기능은 넣지 않는다. 위 충돌 상황에서만 예외적으로 도감이 2개가 되는 것을 허용하고, 평소엔 계정당 도감 1개가 기본이다.
- **닉네임 규칙**: 로그인한 상태에서 도감을 새로 만들면 계정 닉네임을 사용. 비로그인으로 만든 뒤 나중에 로그인해서 캐릭터가 같아 자동 병합되는 경우엔 기기 도감에서 쓰던 닉네임을 그대로 유지. 단, 캐릭터가 달라 위처럼 "대표 도감 지정" 절차를 거치는 경우엔 오너의 닉네임 표기를 계정 닉네임으로 통일한다.
- **다른 진입점에서 충돌이 트리거되는 경우(2-4 "내 도감 만들기")**: "내 도감 만들기"도 로그인을 수반하므로, 이 시점에 uid 연동으로 기존 기기 도감이 발견되면 위와 동일한 절차(캐릭터 같으면 자동 병합 / 다르면 대표 도감 지정)를 그대로 적용한다. 이 경우에도 A(공유 링크를 준 오너)의 도감에 이미 저장된 내 정보는 건드리지 않는다(5장 — 등록 시점 점수 고정). **또한 대표 도감이 정해지는 이 화면에서 A를 자동으로 역등록하지 않는다** — 이 화면은 "어느 게 내 도감이냐"만 정하는 역할로 한정한다. A와의 연결을 대표 도감에 반영하고 싶으면, 이미 로그인·대표 도감이 확정된 상태이므로 A의 공유 링크를 다시 방문해 등록하면 2-4의 "이미 도감 있는 B" 예외(프로필 재사용, 원클릭 등록)로 자연스럽게 처리된다(13번 중복 등록 허용 정책 덕분에 특별한 로직이 필요 없음).

## 4. 참여자 목록 공개 정책

- A의 링크로 들어온 사람은 전체 참여자 목록(닉네임, 캐릭터, **A와의** 케미 점수)을 볼 수 있음(2-3 표 참고, 확정)
- 노출 정보는 닉네임 + 캐릭터 + 점수까지만. 사진·실명 등은 노출하지 않음
- 참여자끼리(B-C)의 케미 점수는 계산·노출하지 않음 — 항상 "A 기준" 점수만 존재
- **중복 등록 허용**: 같은 사람이 여러 기기/시크릿창 등으로 다시 들어와 재등록하거나, 이미 등록한 참여자가 사진을 다시 올려 재등록(다시 뽑기)해도 막지 않는다. 매번 새 엔트리로 쌓이는 걸 그대로 허용한다(사주도령도 동일 정보로 중복 생성을 막지 않음 — 선례를 따름). 중복 방지/동일인 식별 로직은 넣지 않는다.
- **삭제는 단방향**: 오너(A)가 참여자를 자기 도감에서 삭제해도 그 참여자(B) 쪽 도감에 남아있는 "A와 연결됨" 기록에는 영향을 주지 않는다(팔로우/언팔로우와 동일한 개념 — 내 목록에서만 지워짐, 상대 목록엔 그대로 남음)

## 5. 케미 점수 (알고리즘 자체는 이번 범위 아님)

- 계산 로직은 현행 `compatScore()`([functions/engine/compatibility-engine.js](functions/engine/compatibility-engine.js))를 그대로 사용
- **방향성**: A 도감의 엔트리는 `compatScore(A캐릭터, B캐릭터)`(A 기준), B 도감의 엔트리는 `compatScore(B캐릭터, A캐릭터)`(B 기준) — 서로 다른 값이 나올 수 있음(현재 알고리즘 특성 그대로 유지, 이번에 건드리지 않음)
- 사주 반영, 정밀도 개선 등은 별도 과제로 분리

## 6. Firestore 데이터 모델 변경 필요 사항

- `dogam/{slug}/entries/{uid}` 스키마(uid, name, characterId, score, relation, createdAt)는 그대로 유지
- `dogam/{slug}` 문서에 추가 검토:
  - `isPrimary: boolean` — 3장의 대표/비대표 구분용(충돌이 없는 대다수 계정은 도감이 1개뿐이므로 기본값 true)
  - `guestHasOwnDogam: boolean`(entries 문서, 선택) — 참여자가 아직 본인 도감을 안 만든 상태인지 표시용(필수는 아님, UI에서 "아직 도감 없음" 배지 등에 활용 가능)
- `users/{uid}.dogamSlug`는 계정에 도감이 여러 개일 수 있게 되므로 `users/{uid}.dogamSlugs: string[]` + `primarySlug` 형태로 확장 검토 필요(충돌 상황 대비)

## 7. 화면(UI) 요구사항 요약

| 화면 | 구성 |
|---|---|
| 최초 진입 화면(신규) | 기능 소개 + 닉네임 입력 + 사진 업로드 + "관상 캐릭터 뽑기" |
| 로그인 유도 모달(신규) | 도감 생성 직후 1회 노출 — "이 인연도감을 계정에 보관할까요?" + 로그인·회원가입하고 보관하기 / 나중에 할게요 (사주도령 귀인지도 레퍼런스) |
| A 결과 화면 | 로그인 안 했으면 지속 인라인 배너("기기 바뀌면 방장 권한을 잃을 수 있어요" 안내, 사주도령 레퍼런스) + 내 캐릭터 결과 + 공유 링크 버튼 + 참여자 목록(초기엔 빈 상태) |
| B 게스트 뷰(등록 폼) | 상단 A 결과 고정 카드 + 등록 폼(사진/닉네임 + "인연도감 등록하기") + 참여자 목록 — **등록 이력과 무관하게 게스트라면 항상 이 화면**(v4) |
| B 결과 화면(등록 직후, 1회성) | 상단 A 결과 고정 카드 + [B 캐릭터 결과+A기준 케미점수+"내 도감 만들기" CTA] 병합 섹션 + 참여자 목록 — 같은 링크로 재방문하면 다시 위 등록 폼으로 돌아감 |
| 참여자 상세 보기(신규) | 참여자 목록에서 항목 탭 시 등록 당시 케미 상세 재열람 — 재방문 시 자기 결과 재확인 수단 |
| 오너(A) 재방문 뷰 | 내 결과+공유 링크 CTA + 참여자 목록(등록 폼 없음) |
| 로그인 도감 충돌 화면(신규) | "어느 걸 대표로 둘까요?" 라디오 선택 + 비대표 도감 접힘 목록 + 삭제 버튼 |

## 8. 확정된 사항 요약

1. A/B 등록 시 닉네임+사진 둘 다 필수
2. B의 등록 버튼 문구는 "인연도감 등록하기"(A의 최초 버튼과 구분)
3. 케미 점수는 항상 "보는 사람 기준"으로 방향성 있게 계산·저장
4. B 결과 화면은 [내 결과+케미+도감생성 CTA]를 한 섹션으로 병합
5. 모든 방문자 뷰에 등록 폼(미등록자) 또는 내 결과(등록자) + 참여자 전체 목록이 항상 함께 노출
6. "내 도감 만들기"는 로그인 필수. 이미 계정에 도감이 있는 B는 사진 재업로드 없이 기존 프로필로 즉시 등록
7. 로그인 시 캐릭터가 다른 도감 충돌 → 병합하지 않고 "대표 도감 지정" 방식(사주도령 참조), 다중 도감은 이 충돌 상황에서만 예외 허용
8. 참여자 목록은 전체 공개, 케미 점수 순 정렬 + 관계 라벨 필터 칩
9. 오너의 참여자 삭제 기능 추가(단방향 — 상대 도감엔 영향 없음)
10. 만료 정책: 비로그인 도감 30일 자동 만료 / 로그인 계정 도감 무기한
11. 로그인 처리는 익명 계정 credential 연동 방식으로 uid를 유지(신규 uid 발급 금지)
12. 로그인 유도는 생성 직후 모달 1회 + 이후 로그인 전까지 지속되는 인라인 배너 방식(사주도령 레퍼런스)
13. 참여자 중복 등록/재등록(다시 뽑기) 자유 허용 — 동일인 식별·차단 로직 없음
14. 인연도감 등록·케미 계산은 기존처럼 완전 무료 유지(과금 없음, 룰베이스라 AI 비용도 없음)
15. 게스트 뷰는 기기/세션 상태로 "재방문 참여자"를 구분하지 않고 항상 동일한 등록 폼을 보여준다(사주도령 방식, v4) — 공용기기에서 이전 참여자 결과가 노출되는 프라이버시 문제를 막고, 13번(중복 등록 허용) 정책과 일관성 유지

## 9. 후속 확정 필요 (구현 전 세부값)

- **케미 필터 칩 구간**: 일단 기존 관계 라벨(귀인/단짝/내 사람/호랑이 선생님 등, `compatibility-engine.js`에 이미 정의된 라벨)을 그대로 탭으로 재사용해서 우선 진행. 화면이 어느 정도 붙으면 라벨 구성 자체를 다시 검토(현재는 정책 문서 확정을 우선함)
- **필수 동의(약관 체크) 절차**: 검증 과정에서 "이미 구현돼 있다"고 잘못 파악됐던 부분 — 실제로는 미구현 상태로 확인됨. 서비스 정식 오픈을 준비하는 시점에 화면을 실제로 그릴 때 다시 논의하고 반영한다(지금은 보류)

## 10. 비범위 (Out of scope)

- 케미 점수 계산 알고리즘 자체 개선(사주 요소 반영 등)
- 참여자 간(B-C) 케미 점수 계산
- 실명 인증/본인 확인
- "궁합보기(gungham)" 탭과의 통합 — 인연도감과는 계속 별개 기능으로 유지
- 사주도령처럼 언제든 자유롭게 여러 도감을 만드는 완전한 다중 도감 기능(3장 — 충돌 상황 한정 예외로만 허용)
