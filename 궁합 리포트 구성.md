---
문서: 궁합보기 리포트 구성 기준
적용범위: index.html #ggResult (궁합보기 탭 결과 리포트)
최종수정: 2026-08-20
---

## 0. 핵심 원칙

궁합보기는 "이 사람은 어떤 사람인가"가 아니라 **"두 사람의 관계가 어떤가"**만 다룬다.

- 개인별 관상·사주 서술(성격, 강점, 아키타입, 황금타임, 오행 분포 등)은 **통합분석 탭에 이미 있으므로 여기서 반복하지 않는다.**
- 궁합보기에 남기는 콘텐츠는 전부 "두 사람을 비교/조합/블렌드한 결과"여야 한다 — 역할 분담, 얼굴형 조합, 에너지 시너지, 8개 영역 궁합 점수, 신살·귀인 케미 등.
- 새 항목을 추가할 때도 이 기준으로 먼저 판단한다: **한 사람만 봐도 나올 수 있는 내용이면 통합분석행, 두 사람을 같이 봐야만 나오는 내용이면 궁합보기행.**

## 1. 리포트 구성 순서 (위 → 아래, 실제 구현 기준)

**0. 관상 캔버스 카드** — [index.html:1844](index.html:1844) `#ggCanvasCard`
사진을 올린 사람만 나/상대방 얼굴 랜드마크 캡처 이미지가 리포트 카드보다 먼저 노출됨 (사진 없으면 숨김)

**1. 리포트 타이틀** — "👩‍❤️‍👨 AI 관상 & 사주 궁합 리포트" [index.html:1862](index.html:1862)

**2. Zone0 · 종합(히어로) — 기본 펼침** [index.html:1867-1885](index.html:1867)
- 헤드라인 인용구 (`ggHeadline`, `buildCoupleHeadline`)
- 🧠 종합 점수 & 케미 분석: 총점 / 관상만 점수 / 사주만 점수 + AI 사유 텍스트 (`ggHeroReason`)
- 📊 영역별 궁합 점수 & 오행 분포 (`ggScore`/`ggGrade`/`ggDesc`/`ggBars`) — 8개 영역(정서/소통/연애/생활/금전/갈등/성장/장기관계) % 막대바. 예전엔 맨 아래 접힌 디테일이었지만 궁합 점수의 실제 근거 데이터이므로 히어로 안으로 승격하고 항상 펼쳐 보이게 함

**3. Zone1 · 관상으로 보는 우리 케미 — 아코디언(닫힘)** [index.html:1931](index.html:1931) (2026-08-22 순서 개편)
1. 🎭 관상 케미 한줄 총평(`ggRoleTotal`, 신규) — 기존 "관상 궁합 총평"(`chemi.total`)을 역할분담 카드에서 떼어내 Zone1 맨 위 헤드라인격으로 독립
2. 🤝 두 사람의 역할분담 케미(`ggRoleCards`) — `chemi.roleA`/`roleB`만
3. 🌿 관상오행 비교(`ggFaceOhaengCompare` + AI 풀이 `ggZone1AiShape`)
4. 👥 얼굴형·눈·코·입·턱 조합 궁합(`ggFaceComboCards`) — 눈>코>광대뼈>입>턱>얼굴형 순, 각 카드 안 "유형 조합" 보조 줄이 디테일 근거
5. 💰 재물관상 케미(`ggMoneyChemiCard`)
6. ⏳ 생애주기 궁합(`ggLifeStageCard`)

"왜 이렇게 풀이했나요?"(`ggZone1AiShape`·`ggZone2AiItems`의 basis)는 2026-08-22부터 통합분석 탭과 동일하게 `<details class="gg-basis-acc">` 아코디언으로 기본 접힘 처리했다(이전엔 항상 펼쳐져 있었음).

**4. Zone2 · 사주 궁합 — 아코디언(닫힘)** [index.html:1969](index.html:1969) (2026-08-22 순서 개편)
1. 🌡️ 사주 궁합 한줄 총평(`ggOverallRelationship`, 신규) — 기존 `zone2_items`의 `overall_relationship`을 목록에서 떼어내 Zone2 맨 위에 단독 노출
2. ☯️ 만세력 비교(`ggManseryeokCompare`)
3. 🎴 십성으로 보는 서로의 역할(`ggSipseongCross`)
4. 📖 사주 관계 풀이(`ggZone2AiItems`) — `overall_relationship`을 뺀 9개 항목(우리가 끌리는 이유·신살귀인 케미·잘 맞는 부분·관계에서 부족한 부분·상대가 보는 나·내가 보는 상대·상대 마음 사로잡는 법·서로 다르게 자라온 환경·내가 바라는 모습 vs 실제)
5. ☯️ 사주 기운의 케미(`ggEnergyCards`) — 에너지 시너지 + 마음의 안식처
6. 🧭 용신 궁합(`ggYongsinCard`)

**5. Zone3 · 관계 실전 가이드 — 아코디언(닫힘, 2026-08-22 신규)** [index.html](index.html)
1. 🚨 티격태격 모먼트 & 극복 전략(`ggMomentCards`) — 기존 Zone2 소속을 승격 이동. 관계 유형과 무관하게 항상 노출
2. 💌 그래서 우리는 이렇게 만나요(`ggPracticalSection`/`ggPracticalItems`, 신규) — 연애할 때·대화할 때·돈을 다룰 때·싸울 때·오래 만났을 때·결혼했을 때·아이를 낳는다면·우리 관계를 더 좋게 만드는 방법(8항목). **연인/배우자 관계일 때만 노출**(`runGungham()`이 relation으로 hidden 토글) — 친구·가족·지인 관계엔 "아이를 낳는다면" 같은 항목이 어색해서

**6. 인쇄 버튼 / 다른 궁합 분석하기 버튼**

## 2. 제외 항목 (통합분석 탭으로 위임, 궁합보기에서 삭제)

아래는 전부 "개인 서술"이라 궁합보기에서 뺐다. 통합분석 탭(`#cmbResult`)에서 사진·생년월일시를 넣으면 동일한 내용을 확인할 수 있다.

| 제거한 항목 | 예전 위치 | 비고 |
|---|---|---|
| 나의 관상 / 상대방의 관상 서술 문단 | Zone1 (`ggPersonAGwansang`/`B`) | `buildPersonNarrative` 결과 — 통합분석과 동일 소스 |
| 나의 관상 형상 AI 풀이 | Zone1 (`ggZone1AiPersonA`/`B`) | AI 스키마 필드(`zone1_person_a/b_reading·basis`)도 함께 제거 |
| 아키타입(눈모양·전체인상 유형) 카드 | Zone1 (`ggArchetypeA`/`B`) | 개인 관상 형상 분류 카드 |
| 나이 골든타임 | Zone1 (`ggGoldenTimeA`/`B`) | 개인 사주+관상 기반 나이별 조언 |
| 나의 사주 / 상대방의 사주 서술 문단 | Zone2 (`ggPersonASaju`/`B`) | |
| 오행 막대 그래프 | Zone2 (`ggOhaengA`/`B`) | 개인 오행 분포 |
| 오행 심층 리포트 | Zone2 (`ggOhaengDeepDiveA`/`B`) | |

부수 효과: 위 항목을 그리던 개인별 Gemini 호출(`requestPersonalAi('gunghamA'/'B')`)도 함께 없앴다 — 화면에 안 쓰는 API 호출이라 비용·대기시간만 낭비였음. 두 사람 각각의 관상 캐릭터 판정(`classifyAndBuildCharacter`, 룰베이스라 API 비용 없음)은 관상 케미·AI 풀이의 근거 데이터로 계속 필요해서 남겨뒀다.

## 3. 앞으로 리포트를 만들 때 적용할 기준

1. 새 콘텐츠 추가 전에 **"이거 통합분석에도 있는 내용 아닌가?"** 자문 — 있으면 궁합보기에 안 넣는다.
2. Zone0(히어로)에는 **점수·등급류(숫자로 요약되는 것)** 만 놓는다 — 총점/영역별 점수. 서술형 관상·사주 케미는 각 Zone(1/2)에 배치.
3. Zone1은 관상 관련 "조합" 콘텐츠만, Zone2는 사주 관련 "조합" 콘텐츠만, Zone3는 "그래서 실제로 어떻게 만나면 좋은지" 실전 가이드만 — 서로 섞지 않는다.
4. 8개 영역 궁합 점수처럼 **정량적 근거 데이터는 접어두지 말고 히어로에서 바로 보이게** 한다.
5. 헤드라인(한 줄 요약)은 항상 해당 Zone/섹션 맨 위에 온다 — 점수·상세보다 먼저 읽혀야 함.

## 4. Zone2 재편 + Zone3 신설 (2026-08-22)

### 배경

Zone2가 "11개 항목을 그냥 나열"하는 구조라 뭐가 핵심 요약이고 뭐가 상세인지 구분이 안 됐고, "티격태격 모먼트"만 있고 정작 "그래서 실제로 어떻게 만나면 좋은지" 실전 가이드가 없었다.

### Zone2 재편 — 한줄 총평 분리 + 항목 교체

- `overall_relationship`("우리 관계, 한 줄로 말하면")을 `zone2_items` 목록에서 분리해 Zone2 맨 위 헤드라인으로 독립시켰다(Zone1의 "관상 케미 한줄 총평"과 같은 패턴).
- 목록에서 3개를 빼서 Zone3로 옮기고(`after_marriage`→`married_life`, `children`→`children`, `complement_needed`→`improvement`, 아래 참고), 대신 2개를 새로 추가했다:
  - **`attraction_reason`**(우리가 끌리는 이유, 신규) — "무엇이 서로를 끌어당기는가"에 집중, `strengths`(잘 맞는 부분 = 관계가 안정적인 이유)와 겹치지 않게 분리.
  - **`weak_point`**(관계에서 부족한 부분, 신규) — 진단만 하고 해결책은 쓰지 않는다(그건 Zone3의 `improvement`가 맡음).
- 최종 `zone2_items`는 10개: `overall_relationship`(분리 노출)·`attraction_reason`·`sinsal_combo`·`strengths`·`weak_point`·`perceived_by_partner`·`perceived_by_me`·`mind_hacking`·`family_background`·`expectation_vs_reality`.

### Zone3 신설 — "그래서 우리는 이렇게 만나요" (연인/배우자 전용)

`GUNGHAP_ZONE3_ORDER`/`GUNGHAP_ZONE3_META`([js/ai-analysis.js](js/ai-analysis.js)) — 8개 고정 항목, AI가 순서를 정하지 않고 항상 이 순서로 렌더링(Zone4 가변카드의 `ZONE4_TOPIC_ORDER`와 같은 패턴):

| key | 제목 | 비고 |
|---|---|---|
| `dating` | ❤️ 연애할 때 | 기본 디폴트 |
| `communication` | 💬 대화할 때 | 신규 |
| `money` | 💰 돈을 다룰 때 | 신규 |
| `fighting` | 🚨 싸울 때 | 신규 — "티격태격 모먼트"(룰베이스)와 별개로 AI가 씀 |
| `long_term_dating` | 🏠 오래 만났을 때 | 신규 |
| `married_life` | 🏠 결혼했을 때 | 옛 `after_marriage`("결혼 후 우리 모습") 대체 |
| `children` | 👶 아이를 낳는다면 | 옛 Zone2 `children`을 이동 |
| `improvement` | 💡 우리 관계를 더 좋게 만드는 방법 | 옛 Zone2 `complement_needed`("더 채우면 좋은 부분")를 이동 — Zone2 `weak_point`의 진단을 이어받아 실행 조언으로 마무리 |

**연애 기간·혼인 여부를 입력받지 않는다** — 그래서 `long_term_dating`과 `married_life`를 조건 분기 없이 관계가 '연인/배우자'이기만 하면 둘 다 항상 함께 보여준다(사용자 결정: "구분 없이 둘 다 보여주기").

**노출 조건**: Zone3 전체 중 "티격태격 모먼트"는 관계 유형과 무관하게 항상 노출, "그래서 우리는 이렇게 만나요"(`#ggPracticalSection`)는 `state.gungham.relation === '연인/배우자'`일 때만 노출(사용자 결정) — 친구·가족·지인 관계에 "아이를 낳는다면" 같은 항목은 어색하기 때문. `runGungham()`([js/app.js](js/app.js))이 relation 값으로 즉시(AI 응답을 기다리지 않고) `hidden` 클래스를 토글한다.

### 스키마 — 상수 → 함수 전환

`GUNGHAP_REPORT_SCHEMA`(상수)를 `buildGunghapReportSchema(isRomantic)`(함수)로 바꿔, `isRomantic`이 true일 때만 `zone3_practical_items` 필드를 스키마에 추가하고 `required`에도 넣는다 — 통합분석 탭의 `hasSaju && hasFace` 조건부 스키마 필드와 동일한 원칙(§2 판단기준 1번). `state.gungham.cache.isRomantic`(신규, `rel === '연인/배우자'`)이 이 값을 결정한다. `buildGunghapSystemInstruction`도 `isRomantic` 파라미터를 받아 Zone3 작성 지침(장면별 실전 조언에 집중, `married_life`/`long_term_dating` 구분, `improvement`가 `weak_point`를 이어받는 것 등)을 조건부로 덧붙인다.

### 렌더

`renderGunghapResult`([js/ai-analysis.js](js/ai-analysis.js))가 `gunghapItemCardHtml(meta, item)` 공통 헬퍼로 Zone2 목록·Zone3 목록을 같은 `gg-item` 카드 모양으로 그린다. `overall_relationship`은 목록 루프에서 빼서 `#ggOverallRelationship`에 단독 렌더링.
