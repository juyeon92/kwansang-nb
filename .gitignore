functions/node_modules/
node_modules/
.firebase/
firebase-debug.log
firebase-debug.*.log
secret.txt
js.zip
.claude/
사주 추가 분석 데이터 활용 방안.md
캐릭터 톤 예시.md
# Netlify 배포용으로 deploy.ps1이 매번 새로 만드는 폴더 — 원본은 루트에 있으므로 커밋하지 않는다
dist/
# 내부 기획 문서는 저장소에 올리지 않는다(공개 저장소 전환 대비)
기획서/

# 개인 전용 폴더 - 팀원과 공유하지 않는 지침/메모 파일
private-notes/

# Local Netlify folder
.netlify

# Firebase 서비스 계정 키 — 절대 커밋하지 않는다(마이그레이션 스크립트 실행용, 로컬 전용)
functions/serviceAccountKey.json

# Claude Code 로컬 작업 지침 — 저장소엔 안 올림(기획서/private-notes와 같은 성격)
CLAUDE.md

# 스타일 가이드도 내부 실측 문서(Figma 출처·컴포넌트 라이브러리 언급 포함)라 저장소엔 안 올림
STYLE_GUIDE.md
