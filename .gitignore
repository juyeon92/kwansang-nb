functions/node_modules/
.firebase/
firebase-debug.log
firebase-debug.*.log
secret.txt
js.zip
.claude/
사주 추가 분석 데이터 활용 방안.md
캐릭터 톤 예시.md
# Netlify 배포용으로 deploy.ps1이 매번 새로 만드는 폴더 — 원본은 루트에 있으므로 커밋하지 않는다
dist/
# 내부 기획 문서는 저장소에 올리지 않는다(공개 저장소 전환 대비)
기획서/

# 개인 전용 폴더 - 팀원과 공유하지 않는 지침/메모 파일
private-notes/

# Local Netlify folder
.netlify
