
지금의 상황, 주로 어떤 일상을 보내세요, 특별히 더 궁금한 내용
## Part 2 — 이 데이터를 실제 분석에 어떻게 참고할 수 있는지

지금 `q1`(현재 상황: 솔로/연애중/결혼 등), `q2`(일상: 학생/직장인 등), `q3`(자유 질문)은 **저장만 되고 있고, 실제 분석 로직·AI 프롬프트에는 아직 전혀 연결이 안 된 상태**입니다. 연결 지점은 이 셋입니다.

**1. 룰베이스 로직 (`js/app.js`, `js/ai-analysis.js`)**

- 사주 자체 계산(사주팔자, 오행)은 생년월일만으로 정해지는 고정값이라 q1/q2가 그 계산 결과를 바꾸진 않습니다.
- 대신 **해석 문구를 고를 때의 우선순위/필터**로 쓸 수 있어요. 예: 애정운 관련 문구가 여러 후보 중 하나를 골라야 할 때, q1이 "솔로"면 "새로운 인연" 톤을, "결혼했어요"면 "배우자와의 관계" 톤을 우선 노출하는 식. `buildDailyRoutines`, `generatePersonalBehavior` 같은 함수에 `state.combined.q1/q2`를 조건으로 추가하면 됩니다.

**2. Gemini 프롬프트 (`ai-analysis.js`의 `requestPersonalAi`/`requestDeepReport` 계열)**

- 여기가 가장 효과적인 연결 지점입니다. 지금은 사주+관상 데이터만 프롬프트에 넣고 있는데, q1/q2/q3를 프롬프트 컨텍스트에 한 줄 추가하면 AI가 훨씬 개인화된 답을 만듭니다.
    - 예: "이 사람은 현재 솔로이고 직장인입니다. 특히 궁금한 것: 올해 이직운이 있는지" 같은 문장을 프롬프트 앞에 붙이기
    - q3(자유 질문)는 사용자가 직접 쓴 질문이라 **AI가 답변 말미에 그 질문에 대한 직접적인 코멘트를 추가**하도록 지시하면 만족도가 크게 올라갑니다.

**3. 궁합보기 확장 여지**

- 지금 이 질문 세트가 없는 궁합보기에도 나중에 붙이면, "둘 다 직장인" "한쪽은 솔로였다가 새로 만남" 같은 맥락을 궁합 해석에 반영할 수 있습니다 (지금은 범위 밖이라 손 안 댔습니다).
- 